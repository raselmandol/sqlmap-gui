build/
dist/
sqlmap_env/
# Setuptools normalises the dist name to sqlmap_gui.egg-info; match any name.
*.egg-info/
# Recursive on its own -- covers every package subdirectory.
__pycache__/
sqlmap-gui.spec

# sqlmap (download))
sqlmap/